<div class="left_margin">
	<% if flash[:alert] %>
		<p><%= flash[:alert] %></p>
	<% end %>

	<h1>Sign In</h1>
	<%= simple_form_for :session, :url => sessions_path do |f| %>
		<%= f.input :email %>
		<%= f.input :password %>
		<%= f.submit 'Log In' %>
	<% end %>
</div>